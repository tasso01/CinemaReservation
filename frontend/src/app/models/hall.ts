export class Hall {
  id: number;
  number: number;
  capacity: number;

  constructor(id: number, number: number, capacity: number) {
    this.id = id;
    this.number = number;
    this.capacity = capacity;
  }
}
